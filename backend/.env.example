# Backend Environment Variables Template
# Copy this file to .env and configure with your values

# ======================
# Server Configuration
# ======================
PORT=3001
NODE_ENV=development

# ======================
# Database Configuration
# ======================
# For Local Development: use localhost (SSL disabled automatically)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/scraper_db

# For Docker: use postgres as hostname (SSL disabled automatically)
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/scraper_db

# For Production: use remote host (SSL enabled automatically)
# DATABASE_URL=postgresql://user:password@remote-host:5432/dbname

# ======================
# Redis Configuration
# ======================
# For Local Development: use localhost
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# For Docker: use redis as hostname
# REDIS_HOST=redis
# REDIS_PORT=6379

# ======================
# Google Gemini AI
# ======================
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# Get your API key from: https://aistudio.google.com/app/apikey
# Available models:
#   - gemini-2.0-flash-exp (recommended, fastest)
#   - gemini-1.5-pro (most capable)
#   - gemini-1.5-flash (balanced)

# ======================
# Scraper Configuration
# ======================
MAX_CONTENT_LENGTH=50000
# Maximum characters to extract from scraped websites
# Recommended: 30000-100000 depending on your needs

USE_PLAYWRIGHT=false
# Set to 'true' to use Playwright browser automation (better accuracy)
# Set to 'false' to use HTTP scraper with axios + cheerio (lightweight)
# Note: Playwright requires browser installation (playwright install chromium)


